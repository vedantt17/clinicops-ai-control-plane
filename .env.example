# The public simulation runs without credentials.
# Optional production adapters must be supplied at runtime and never committed.
FHIR_BASE_URL=
FHIR_CLIENT_ID=
FHIR_CLIENT_SECRET=
# Optional server-side live evaluation adapter. The public deployment uses replay mode.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
